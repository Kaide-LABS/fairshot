"""
Auto-generated middleware: {{ ats_name }} → Fairshot API
Generated by Virtual FDE Code Generator
"""

from datetime import datetime, timezone
from typing import Any, Optional
import json


# ─── Transform Helpers ───────────────────────────────────────────────────

def _safe_get(record: dict, path: str, default: Any = None) -> Any:
    """Safely extract a value from a nested dict using dot-notation path."""
    keys = path.split(".")
    current = record
    for key in keys:
        if isinstance(current, dict) and key in current:
            current = current[key]
        else:
            return default
    return current


def _direct_copy(value: Any) -> Any:
    return value


def _lowercase_enum(value: Any, enum_map: dict | None = None) -> str | None:
    if value is None:
        return None
    if enum_map and str(value) in enum_map:
        return enum_map[str(value)]
    return str(value).lower().replace("_", "-") if value else None


def _date_mmddyyyy_to_iso(value: str | None) -> str | None:
    if not value:
        return None
    try:
        dt = datetime.strptime(value, "%m/%d/%Y")
        return dt.strftime("%Y-%m-%d")
    except (ValueError, TypeError):
        return value


def _epoch_to_iso(value: int | float | None) -> str | None:
    if value is None:
        return None
    try:
        dt = datetime.fromtimestamp(int(value), tz=timezone.utc)
        return dt.strftime("%Y-%m-%d")
    except (ValueError, TypeError, OSError):
        return None


def _semicolon_to_list(value: str | None) -> list[str]:
    if not value:
        return []
    return [item.strip() for item in value.split(";") if item.strip()]


def _json_string_to_list(value: str | None) -> list[str]:
    if not value:
        return []
    try:
        parsed = json.loads(value)
        return parsed if isinstance(parsed, list) else []
    except (json.JSONDecodeError, TypeError):
        return []


def _phone_to_e164(value: str | None) -> str | None:
    if not value:
        return None
    cleaned = ""
    for ch in value:
        if ch.isdigit() or (ch == "+" and not cleaned):
            cleaned += ch
    return cleaned if cleaned else None


def _numeric_enum(value: Any, enum_map: dict | None = None) -> str | None:
    if value is None:
        return None
    if enum_map and str(value) in enum_map:
        return enum_map[str(value)]
    return str(value)


def _set_nested(d: dict, path: str, value: Any) -> None:
    """Set a value in a nested dict using dot-notation path."""
    keys = path.split(".")
    for key in keys[:-1]:
        if key not in d:
            d[key] = {}
        d = d[key]
    d[keys[-1]] = value


def _apply_transform(value: Any, transform_type: str, params: dict | None = None) -> Any:
    """Apply a named transform to a value."""
    if params is None:
        params = {}
    if transform_type == "direct_copy":
        return _direct_copy(value)
    elif transform_type == "lowercase_enum":
        return _lowercase_enum(value, params.get("enum_map"))
    elif transform_type == "date_mmddyyyy_to_iso":
        return _date_mmddyyyy_to_iso(value)
    elif transform_type == "epoch_to_iso":
        return _epoch_to_iso(value)
    elif transform_type == "nested_extract":
        return value
    elif transform_type == "semicolon_to_list":
        return _semicolon_to_list(value)
    elif transform_type == "json_string_to_list":
        return _json_string_to_list(value)
    elif transform_type == "phone_to_e164":
        return _phone_to_e164(value)
    elif transform_type == "numeric_enum":
        return _numeric_enum(value, params.get("enum_map"))
    else:
        return value


# ─── Main Transform Function ────────────────────────────────────────────

def transform(record: dict) -> dict:
    """Transform a raw {{ ats_name }} record into a Fairshot API payload."""
    output = {}

    # ─── Scalar Transforms ───────────────────────────────────────────
{% for t in transforms %}
{% if not t.is_array_item %}
    _val = _safe_get(record, "{{ t.ats_source_path }}")
{% if t.nullable %}
    if _val is not None:
        _set_nested(output, "{{ t.fairshot_field }}", _apply_transform(_val, "{{ t.transform_type }}", {{ t.params | tojson }}))
{% else %}
    _set_nested(output, "{{ t.fairshot_field }}", _apply_transform(_val, "{{ t.transform_type }}", {{ t.params | tojson }}))
{% endif %}
{% endif %}
{% endfor %}

    # ─── Array Transforms ────────────────────────────────────────────
{% set array_groups = {} %}
{% for t in transforms if t.is_array_item %}
{% if t.array_target_path not in array_groups %}
{% set _ = array_groups.update({t.array_target_path: []}) %}
{% endif %}
{% set _ = array_groups[t.array_target_path].append(t) %}
{% endfor %}
{% for target_array, items in array_groups.items() %}
    _source_arr = _safe_get(record, "{{ items[0].array_source_path }}", [])
    if isinstance(_source_arr, list):
        _target_arr = []
        for _item in _source_arr:
            _mapped = {}
{% for t in items %}
            _arr_val = _item.get("{{ t.ats_source_path.split('.')[-1] }}")
            if _arr_val is not None:
                _mapped["{{ t.fairshot_field.split('.')[-1] }}"] = _apply_transform(_arr_val, "{{ t.transform_type }}", {{ t.params | tojson }})
{% endfor %}
            _target_arr.append(_mapped)
        output["{{ target_array }}"] = _target_arr
{% endfor %}

{% for block in custom_code_blocks %}
    {{ block }}
{% endfor %}

    return output
